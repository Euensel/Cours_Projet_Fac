/*
    ->   une spécification est un ensemble de règle qui montre comment les choses fonctionnent ou sont mises en application au sein de différents outils que nous utilisons(le naviagateur par exemple en web)

    Javascript quelques specification
        -Object,Array,Math,Set,etc...(premiere partie)
        -BOM(Browser Object Model) //concerne le navigateur
            windows.document(DOM(Document Object Model))
            windows.screen
            window.navigator
            window.history
            window.location
        -API Canvas
        -API Fetch
        -XMLHttpRequest

 */

